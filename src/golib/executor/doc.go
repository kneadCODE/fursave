// Package executor provides utilities for running multiple services concurrently
// with graceful shutdown handling. It ensures proper cleanup and shutdown sequence
// when one service fails or when the application receives a termination signal.
package executor
